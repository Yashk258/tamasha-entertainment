# 🎭 Tamasha Entertainment

A modern and interactive entertainment platform built for **Tamasha Entertainment**, designed to showcase events, experiences, and entertainment services through a visually engaging web experience.

## Tech Stack

* **TanStack Start** — Full-stack React framework
* **React** — UI development
* **TypeScript** — Type-safe development
* **Tailwind CSS** — Styling and responsive design
* **Vite** — Development and build tooling

## Project Structure

```text
Frontend/
├── public/
├── src/
│   ├── routes/
│   │   ├── __root.tsx
│   │   ├── index.tsx
│   │   ├── about.tsx
│   │   └── routeTree.gen.ts
│   └── ...
├── package.json
├── tsconfig.json
└── README.md
```

## Routes

This project uses **TanStack Start file-based routing**.

Every `.tsx` file inside `src/routes/` defines a route.

| File                     | URL                 |
| ------------------------ | ------------------- |
| `index.tsx`              | `/`                 |
| `about.tsx`              | `/about`            |
| `users/index.tsx`        | `/users`            |
| `users/$id.tsx`          | `/users/:id`        |
| `posts/{-$category}.tsx` | `/posts/:category?` |
| `files/$.tsx`            | `/files/*`          |

### Route Conventions

* `index.tsx` → Root route
* `$id.tsx` → Dynamic route
* `{-$category}.tsx` → Optional route parameter
* `$.tsx` → Splat/catch-all route
* `_layout.tsx` → Layout route
* `__root.tsx` → Application root layout

Layout routes should render their child routes using `<Outlet />`.

> **Note:** Do not create `src/pages/`, `src/routes/_app/index.tsx`, or `app/layout.tsx`. These follow conventions from other frameworks such as Next.js and Remix.

## Installation



```bash
git clone https://github.com/Yashk258/tamasha-experience-forge.git
```

Navigate to the frontend directory:

```bash
cd Frontend
```

Install dependencies:

```bash
npm install
```

## 💻 Development

Start the development server:

```bash
npm run dev
```

The application will be available at the local development URL shown in your terminal.

## 🏗️ Production Build

Create a production build:

```bash
npm run build
```

## 🔄 Git Workflow

After making changes:

```bash
git add .
git commit -m "Update project"
git push
```

## Generated Files

`src/routeTree.gen.ts` is automatically generated by TanStack Start.

**Do not edit this file manually.**

The route tree is regenerated automatically when routes are created, removed, or modified.

## 🎯 Project Goals

* Create a modern entertainment platform
* Provide an engaging user experience
* Showcase Tamasha Entertainment events and experiences
* Maintain responsive design across devices
* Use scalable and maintainable frontend architecture

## 👨‍💻 Author

**Yash Kumar**

B.Tech Computer Science & Engineering

---

⭐ If you like this project, consider giving the repository a star!
